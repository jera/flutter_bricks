# Objetivo
- Construirmos uma brick para automatizar a criação da feature de eventos dos app (firebase & mixpanel);
- A brick deve gerar o arquivo e importar os packages necessários;
- A brick deve ter hooks para adicionar as libs  automaticamente;
- Testarmos a brick no projeto criado "dojo_events"


### Opcionais:
- Adicionarmos o opcional de gerar só firebase ou só mixPanel;
- Adicionarmos opção de variaveis para config exemplo = MixPanelToken;