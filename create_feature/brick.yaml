name: create_feature
description: A brick for create features.
repository: https://github.com/jaumpelicon/my-mason-bricks/tree/master/create_feature

version: 0.2.2+2

environment:
  mason: ">=0.1.0-dev.47 <0.1.0"

vars:
  name:
    type: string
    description: Feature name generate
    prompt: Qual o nome da sua feature?
  has_factory:
    type: boolean
    description: if true generate di
    prompt: Deseja gerar o arquivo factory?
